# Calcit 0.13.63 compatibility

- Migrated the legacy `when-let` macro schema with the final compatible Calcit
  0.13.51 toolchain, then declared its required syntax, rest syntax, expansion,
  and capabilities explicitly for current strict Snapshot loading.
- Kept `ws-send!` and `wss-send!` consistent with their public `Unit` contracts
  by returning `&unit` after the JavaScript boundary.
- Aligned local dependencies and CI with Calcit 0.13.63, including the JS async
  FFI proc exports required for warning-free browser bundling.
- Aligned the previously missing/mismatched module version metadata at 0.0.17.
- Documented the strict macro and WebSocket send return contracts.

Validation covers strict Snapshot loading, both page/server JavaScript entries,
unit tests, deprecated API analysis, immutable Yarn install, and the Vite build.
