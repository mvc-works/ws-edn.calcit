# Calcit 0.13.64 upgrade / 升级 Calcit 0.13.64

## 中文

- 将 `deps.cirru` 与 `@calcit/procs` 同步升级到 0.13.64，并将 npm 约束固定为精确版本，避免 CLI 与 JS runtime 漂移。
- 保持页面与 server WebSocket API、strict macro contracts 及既有 `Unit` send 语义不变。
- 通过 page/server check-only、unit tests、deprecated API 检查、双入口 JS codegen 与 Node 24 Vite build 验证。

## English

- Upgrade `deps.cirru` and `@calcit/procs` together to 0.13.64, pinning the npm dependency exactly to prevent CLI/JS runtime drift.
- Keep the page/server WebSocket API, strict macro contracts, and existing `Unit` send semantics unchanged.
- Verify with page/server check-only, unit tests, deprecated API analysis, both JS codegen entries, and the Node 24 Vite build.
