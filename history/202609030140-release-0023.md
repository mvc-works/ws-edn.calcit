# 0.0.23

- Align public `WsClient` trait operations with internal `WsClient0` state helpers under Calcit 0.13.75.
- Replace the server callback nil sentinel with `Option` and exhaustively match missing sockets.
- Use cumulo-util 0.0.15 and enforce the native Cirru EDN quality baseline with zero dynamic dispatch.
- 对齐 `WsClient` trait 与内部 `WsClient0` 状态边界，移除 server callback 的 nil 哨兵并用 Option 建模。
