# Release 0.0.20

- Publishes the generation-gated browser lifecycle recovery stage from PR #27.
- Keeps the Calcit package version in `deps.cirru` and the JavaScript package
  version in `package.json` aligned for downstream tag-based resolution.
