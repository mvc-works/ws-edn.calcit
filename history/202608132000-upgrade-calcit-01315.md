# Calcit 0.13.15 and typed WebSocket payloads

- Upgraded the snapshot and `@calcit/procs` from 0.13.13 to 0.13.15.
- Added a nominal `Track` schema and `decode-track` helper backed by
  `decode-map-as`, with tests for valid data and missing required fields.
- Tightened `ws-connected?`, `ws-send!`, and `wss-send!` schemas; explicit Unit
  returns now keep JS FFI results from leaking into application code.
- Kept the two native entries (`default` and `server`) and verified both
  check-only and JavaScript code generation paths.
- Added documentation for the typed boundary and removed-module policy.

Validation: both entries pass `--check-only`, page/server JS codegen passes,
unit tests pass 2/2, and deprecated API analysis reports zero calls.
