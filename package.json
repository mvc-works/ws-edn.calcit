{
  "version": "0.0.7",
  "dependencies": {
    "@calcit/procs": "^0.5.28",
    "nanoid": "^3.3.2"
  },
  "scripts": {
    "watch-server": "cr --emit-js --entry server",
    "compile-server": "cr --emit-js --once --entry server",
    "compile-page": "cr --emit-js --once --emit-path out-page/",
    "build-page": "vite build --base=./",
    "watch-page": "cr --emit-js --emit-path out-page/"
  },
  "devDependencies": {
    "vite": "^2.9.0",
    "ws": "^8.5.0"
  }
}
