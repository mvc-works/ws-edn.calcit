{
  "version": "0.0.10",
  "dependencies": {
    "@calcit/procs": "^0.9.13",
    "nanoid": "^5.1.5"
  },
  "scripts": {
    "watch-server": "cr --entry server js",
    "compile-server": "cr --entry server -1 js",
    "compile-page": "cr --emit-path out-page/ -1 js",
    "build-page": "vite build --base=./",
    "watch-page": "cr --emit-path out-page/ js"
  },
  "devDependencies": {
    "vite": "^7.0.0",
    "ws": "^8.18.2"
  }
}
